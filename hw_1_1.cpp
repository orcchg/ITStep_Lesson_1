#include <cstdio>

int main(int argc, char** argv) {

    int a = 5;
    int b = 7;
    int c = a + b;
    int d = a * b;

    printf("%i + %i = %i\n", a, b, c);

    printf("%i * %i = %i\n", a, b, d);

    return 0;
}
